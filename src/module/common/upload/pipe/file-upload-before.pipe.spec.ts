import { FileUploadBeforePipe } from './file-upload-before.pipe';

describe('FileUploadBeforePipe', () => {
  it('should be defined', () => {
    expect(new FileUploadBeforePipe()).toBeDefined();
  });
});
